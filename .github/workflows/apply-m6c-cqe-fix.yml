name: Apply Milestone 6C CQE Fix

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    name: Apply CQE validation bundle
    runs-on: ubuntu-latest
    steps:
      - name: Check out head branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          persist-credentials: true

      - name: Apply and commit CQE validation
        run: |
          base64 -d .m6c/cqe-fix.tar.gz.b64 > "$RUNNER_TEMP/m6c-cqe-fix.tar.gz"
          tar -xzf "$RUNNER_TEMP/m6c-cqe-fix.tar.gz"
          rm -rf .m6c
          rm .github/workflows/apply-m6c-cqe-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Harden io_uring CQE validation"
          git push origin "HEAD:${{ github.head_ref }}"
